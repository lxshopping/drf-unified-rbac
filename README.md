# drf-unified-rbac

Reusable RBAC component for Django REST Framework.

Current milestone:

- V0.1 Local RBAC
- Django User -> Role -> Permission
- DRF permission integration

Future milestones:

- V0.2 OIDC / Keycloak
- External role mapping
- Object/data scope permissions
