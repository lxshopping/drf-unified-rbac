[pytest]
DJANGO_SETTINGS_MODULE = tests.settings
# Install the app (editable for development, wheel for distribution checks).
pythonpath = example_project .
testpaths = tests
addopts = --strict-markers
